import { createTheme } from '@mui/material/styles';

export const themeOptions = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#051367',
    },
    secondary: {
      main: '#2D31FA',
    },
  },
});